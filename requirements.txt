alpaca-py>=0.21.0
requests>=2.28.0
feedparser>=6.0.0
numpy>=1.24.0
pandas>=2.0.0
streamlit>=1.35.0
plotly>=5.20.0
scipy>=1.11.0
pytz>=2024.1

# ── Optional advanced ML features ──────────────────────────────────────
scikit-optimize>=0.9.0   # Bayesian strategy weight optimiser
arch>=5.3.0              # GARCH(1,1) crypto volatility
transformers>=4.30.0     # FinBERT sentiment analysis
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0+cpu         # FinBERT inference backend (CPU-only wheel, ~200 MB vs 2 GB GPU)
